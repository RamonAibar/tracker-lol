services:
  - type: web
    name: tracker-lol
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: RIOT_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
